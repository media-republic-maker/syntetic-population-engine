
  # Synthetic Population Sandbox UI

  This is a code bundle for Synthetic Population Sandbox UI. The original project is available at https://www.figma.com/design/2btGNoasUY2rvNY63WHoIV/Synthetic-Population-Sandbox-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  